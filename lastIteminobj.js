let obj = { name: 'Ajay', age: 25, city: 'New Delhi' }
// To ge last item from object
const keys = Object.keys(obj);
console.log(keys[keys.length-1])
const values = Object.values(obj)

console.log(values[values.length-1])
