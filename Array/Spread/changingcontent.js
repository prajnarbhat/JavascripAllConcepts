// Create a new object updatedUser using the spread operator 
// where city is changed to "Los Angeles".

const user = { name: "Alice", age: 25, city: "New York" };

const user1 = {...user, city: "Chenail"}

console.log(user1);