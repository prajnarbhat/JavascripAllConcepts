// array join method create new string from an array
// I will concat all the elements from the array and create a new string
// by default it take , as seperator
// if you want to change the separator you can pass it as argument

const countries = ["India","Europe","Indonesia"]

const res = countries.join(", ")

console.log(res);